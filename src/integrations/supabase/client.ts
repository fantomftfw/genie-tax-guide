
// This is a placeholder file to prevent import errors
// We've removed actual Supabase functionality and switched to local auth

const mockClient = {
  // Empty mock client to prevent build errors
};

export const supabase = mockClient;
